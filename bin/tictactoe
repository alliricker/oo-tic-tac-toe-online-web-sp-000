#!/usr/bin/env ruby
require 'pry'
require_relative '../lib/tic_tac_toe.rb'

def play
  until over? || draw?
    turn
  end
  if won?
    winner == "X" || winner == "O"
    puts "Congratulations #{winner}!"
  else draw?
    puts "Cat\'s Game!"
  end
end
